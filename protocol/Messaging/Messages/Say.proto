syntax = "proto3";

package protocol.Messaging.Messages;

// Say определяет сообщение в чат
message Say {
	// sender определяет идентификатор отправителя сообщения
	int32 sender = 1;
	// text определяет текст сообщения
    string text = 2;
}
